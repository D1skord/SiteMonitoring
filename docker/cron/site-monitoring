SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

*/5 * * * * root cd /var/www/symfony && { echo "$(date -Is) site:check-status started"; /usr/local/bin/php bin/console site:check-status --env=prod; echo "$(date -Is) site:check-status finished"; } >> /proc/1/fd/1 2>> /proc/1/fd/2
0 3 * * * root cd /var/www/symfony && { echo "$(date -Is) site:check-ssl-expire started"; /usr/local/bin/php bin/console site:check-ssl-expire --env=prod; echo "$(date -Is) site:check-ssl-expire finished"; } >> /proc/1/fd/1 2>> /proc/1/fd/2
20 3 * * * root cd /var/www/symfony && { echo "$(date -Is) site:check-domain-expire started"; /usr/local/bin/php bin/console site:check-domain-expire --env=prod; echo "$(date -Is) site:check-domain-expire finished"; } >> /proc/1/fd/1 2>> /proc/1/fd/2
